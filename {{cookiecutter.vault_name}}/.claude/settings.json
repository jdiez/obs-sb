{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/build-index.sh"
          },
          {
            "type": "command",
            "command": "echo 'STARTUP TASK: Check inbox/ for unprocessed notes. If any files exist (excluding CLAUDE.md), process each one: determine the best category and subjects, add YAML frontmatter with [[wikilink]] properties, and move to notes/ (or journal/YYYY/MM/ for journal entries, using year-based hierarchy). Report what was processed. If inbox is empty, say so briefly and move on.'"
          },
          {
            "type": "command",
            "command": "bash .claude/hooks/vault-hygiene.sh"
          },
          {
            "type": "command",
            "command": "bash .claude/hooks/review-overdue.sh"
          },
          {
            "type": "command",
            "command": "bash .claude/hooks/journal-reflections.sh"
          },
          {
            "type": "command",
            "command": "bash .claude/hooks/wiki-lint.sh"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/uncommitted-changes.sh"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'system/templates/'; then echo 'BLOCK: Template files are protected. Ask before modifying.' >&2; exit 2; fi"
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'system/templates/'; then echo 'BLOCK: Template files are protected. Ask before modifying.' >&2; exit 2; fi"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/journal-reminder.sh"
          },
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'notes/'; then bash .claude/hooks/build-index.sh 2>/dev/null; fi"
          }
        ]
      },
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/journal-reminder.sh"
          },
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'notes/'; then bash .claude/hooks/build-index.sh 2>/dev/null; fi"
          }
        ]
      }
    ]
  }
}
